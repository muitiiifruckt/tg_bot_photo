# Telegram Bot для генерации изображений

Telegram бот для генерации изображений через OpenRouter API с системой внутренней валюты (рубины) и интеграцией ЮКассы для пополнения баланса.

## Возможности

- 🎨 Генерация изображений через OpenRouter API (модель Google Gemini 3 Pro Image Preview)
- 💎 Система внутренней валюты (рубины)
- 💳 Пополнение баланса через ЮКассу (СБП)
- 👤 Профили пользователей с историей генераций
- 📊 Отслеживание расходов и платежей

## Установка

1. Клонируйте репозиторий или скачайте файлы проекта

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Заполните переменные окружения в файле `.env`:
   - `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота (получите у @BotFather)
   - `OPENROUTER_API_KEY` - API ключ от OpenRouter (получите на https://openrouter.ai)
   - `YOOKASSA_SHOP_ID` - ID магазина в ЮКассе
   - `YOOKASSA_SECRET_KEY` - Секретный ключ ЮКассы
   - `PRICE_PER_GENERATION` - Стоимость одной генерации в рубинах (по умолчанию 10)

## Настройка

### Получение Telegram Bot Token

1. Откройте Telegram и найдите бота @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env`

### Получение OpenRouter API Key

1. Зарегистрируйтесь на https://openrouter.ai
2. Перейдите в раздел API Keys
3. Создайте новый API ключ
4. Скопируйте ключ в `.env`

### Настройка ЮКассы

1. Зарегистрируйтесь в ЮКассе (https://yookassa.ru)
2. Создайте магазин и получите Shop ID и Secret Key
3. В настройках магазина укажите URL для webhook (опционально)
4. Скопируйте Shop ID и Secret Key в `.env`

## Запуск

```bash
python bot.py
```

## Использование

### Команды бота

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/profile` - Посмотреть свой профиль и баланс
- `/buy` - Купить рубины
- `/generate` - Инструкция по генерации изображений

### Генерация изображений

Просто отправьте боту текстовое описание изображения, которое вы хотите сгенерировать. Например:
- "Красивый закат над горами"
- "Кот в космосе"
- "Футуристический город"

Бот автоматически сгенерирует изображение и спишет рубины с вашего баланса.

### Пополнение баланса

1. Используйте команду `/buy`
2. Выберите пакет рубинов
3. Нажмите "Оплатить" для перехода к оплате через СБП
4. После оплаты нажмите "Проверить оплату"

## Структура проекта

```
tg_photo_bot/
├── bot.py                 # Основной файл бота
├── config.py              # Конфигурация
├── database.py            # Работа с базой данных
├── openrouter_client.py   # Клиент для OpenRouter API
├── yookassa_payment.py    # Интеграция с ЮКассой
├── requirements.txt       # Зависимости
├── .env.example          # Пример файла с переменными окружения
├── README.md             # Документация
└── bot_database.db       # База данных SQLite (создается автоматически)
```

## База данных

Бот использует SQLite для хранения данных. База данных создается автоматически при первом запуске.

Таблицы:
- `users` - информация о пользователях и их балансе рубинов
- `payments` - история платежей
- `generations` - история генераций изображений

## Примечания

- Убедитесь, что у вас достаточно средств на балансе OpenRouter для генерации изображений
- Для продакшена рекомендуется настроить webhook для автоматической проверки платежей
- В файле `bot.py` в функции `create_payment` замените `return_url` на URL вашего бота

## Лицензия

MIT
